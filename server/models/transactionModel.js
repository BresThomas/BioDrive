class Transaction {
    constructor(id_transaction, paiement, carburant) {
        this.id_transaction = id_transaction;
        this.paiement = paiement;
        this.carburant = carburant;
    }
}

export default Transaction;
